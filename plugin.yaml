name: memproject
version: 1.0.0
description: "MemProjectProvider — long-running task management with per-task Git repos, human-in-the-loop approval gates, and cross-session recovery."
pip_dependencies: []
hooks:
  - on_turn_start
  - on_session_end
  - on_pre_compress
  - on_memory_write
